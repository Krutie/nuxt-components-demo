{
  "name": "nuxt-components-example",
  "version": "1.0.0",
  "description": "Nuxt component module example",
  "author": "Krutie Patel",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "rm -rf dist && nuxt generate && cd dist && surge ./ --domain http://nuxt-components.surge.sh"
  },
  "dependencies": {
    "nuxt-edge": "^2.13.0-26537781.68d498b2",
    "vue-awesome": "^4.0.2"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^2.0.0"
  }
}
